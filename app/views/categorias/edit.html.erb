$("#formModal").html("<%= escape_javascript(render partial: 'form_modal', 
                                                     locals: { categoria: @categoria } ) %>");
$("#formModal").modal("show");